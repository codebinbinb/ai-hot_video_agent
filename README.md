# Skyline AI Spec Coding

AI 辅助的规范化开发工具包，提供完整的 10 步开发流程工作流。

## 概述

本项目提供一套 Windsurf IDE 工作流（Workflow），覆盖从需求分析到交付报告的完整开发周期，帮助团队实现规范化、可追溯的软件开发。

## 10 步开发流程

```mermaid
flowchart LR
    subgraph 需求阶段
        A1["① 需求分析"] --> A2["② 产品设计"]
    end
    
    subgraph 设计阶段
        A2 --> B1["③ 架构设计"]
        B1 --> B2["④ 开发计划"]
    end
    
    subgraph 开发阶段
        B2 --> C1["⑤ 任务分配"]
        C1 --> C2["⑥ 执行开发"]
        C2 --> C3["⑦ 单元测试"]
    end
    
    subgraph 测试阶段
        C3 --> D1["⑧ 测试用例"]
        D1 --> D2["⑨ 测试报告"]
    end
    
    subgraph 交付阶段
        D2 --> E1["⑩ 开发报告"]
    end
```

## Workflow 使用方法

在 Windsurf IDE 中，使用 `/` 命令调用工作流：

### 核心流程

| 命令 | 步骤 | 说明 | 产出物 | 归档目录 |
|------|------|------|--------|----------|
| `/speckit.demand` | ① | 需求分析 | `{功能}-需求规格.md` | `docs/demand/` |
| `/speckit.product` | ② | 产品设计 | `{功能}-产品设计-V{版本}.md` | `docs/product/` |
| `/speckit.architecture` | ③ | 架构设计 | `{功能}-架构设计-V{版本}.md` | `docs/architecture/` |
| `/speckit.plan` | ④ | 开发计划 | `{功能}-开发计划.md` | `docs/plan/` |
| `/speckit.tasks` | ⑤ | 任务分配 | `{功能}-任务清单.md` | `docs/plan/` |
| `/speckit.implement` | ⑥ | 执行开发 | 代码实现 | `backend/` `frontend/` |
| `/speckit.unittest` | ⑦ | 单元测试 | 单元测试代码 | `tests/` |
| `/speckit.testcase` | ⑧ | 测试用例 | `{功能}-测试用例.md` | `docs/test/` |
| `/speckit.testreport` | ⑨ | 测试报告 | `{功能}-测试报告.md` | `docs/test/` |
| `/speckit.report` | ⑩ | 开发报告 | `{功能}-开发报告.md` | `docs/report/` |

### 辅助工具

| 命令 | 说明 |
|------|------|
| `/speckit.clarify` | 澄清需求中的不明确点 |
| `/speckit.constitution` | 创建或更新项目规约 |
| `/speckit.c4model` | 生成 C4 Model 架构文档 |
| `/speckit.update` | 🔄 需求变更时智能增量更新受影响文档 |
| `/speckit.archive` | 📦 迭代完成后归档文档快照 |

### 逆向工程技能

基于 Playwright MCP 浏览器代理，对已有系统进行功能逆向梳理：

| 命令 | 阶段 | 说明 | 产出物 |
|------|------|------|--------|
| `/speckit.reverse-scan` | 层级扫描 | 扫描系统界面，构建层级图谱 | `hierarchy-graph.md` |
| `/speckit.reverse-traverse` | 全量遍历 | 逐节点操作，采集截图和接口 | `screenshots/`, `api-requests.json` |
| `/speckit.reverse-doc` | 文档生成 | 整合遍历结果，生成结构化文档 | 功能手册、接口映射、任务清单 |

**核心原则**：所见即所得，无可视化证据不记录

## 快速开始

### 1. 启动新功能开发

```bash
# 在 Windsurf 中输入
/speckit.demand 用户登录功能，支持手机号和邮箱两种方式
```

这将：
- 创建功能分支 `001-user-login`
- 生成需求规格文档
- 归档到 `docs/demand/`

### 2. 按流程推进

```bash
# 产品设计
/speckit.product

# 架构设计（指定技术栈）
/speckit.architecture 使用 Vue3 + FastAPI + PostgreSQL

# 生成开发计划
/speckit.plan

# 拆分任务
/speckit.tasks

# 开始开发
/speckit.implement
```

### 3. 测试与交付

```bash
# 编写单元测试
/speckit.unittest

# 生成测试用例
/speckit.testcase

# 执行测试并生成报告
/speckit.testreport

# 生成开发报告
/speckit.report
```

## 目录结构

```
.
├── .specify/                    # Spec-Kit 配置目录
│   ├── memory/                  # 项目记忆和规约
│   │   ├── constitution.md      # 项目规约
│   │   └── c4-model-constitution.md
│   ├── specs/                   # 功能规格（按分支组织）
│   │   └── 001-user-login/
│   │       ├── spec.md
│   │       ├── product.md
│   │       ├── architecture.md
│   │       ├── plan.md
│   │       └── tasks.md
│   └── templates/               # 文档模板
├── .windsurf/
│   └── workflows/               # Windsurf 工作流定义
├── docs/                        # 归档文档
│   ├── demand/                  # ① 需求规格
│   ├── product/                 # ② 产品设计
│   ├── architecture/            # ③ 架构设计
│   ├── plan/                    # ④⑤ 计划与任务
│   ├── test/                    # ⑧⑨ 测试文档
│   └── report/                  # ⑩ 开发报告
├── backend/                     # 后端代码
├── frontend/                    # 前端代码
└── tests/                       # 测试代码
```

## 规约文件

- `.specify/memory/constitution.md` - 项目开发规约，定义代码质量、架构原则等
- `.specify/memory/c4-model-constitution.md` - C4 架构文档规范
- `.specify/memory/reverse-engineering-constitution.md` - 逆向工程规约

## 逆向工程使用示例

对已有系统进行功能梳理和接口文档补全：

```bash
# 1. 层级扫描 - 构建系统功能图谱
/speckit.reverse-scan https://example.com/admin 用户名 密码

# 2. 全量遍历 - 逐节点采集截图和接口
/speckit.reverse-traverse

# 3. 文档生成 - 输出结构化功能文档
/speckit.reverse-doc
```

**输出目录结构**：

```
docs/reverse/{系统名称}-{日期}/
├── index.md                    # 文档索引
├── hierarchy-graph.md          # Mermaid 层级图谱
├── hierarchy-structure.md      # 层级结构表
├── traversal-paths.md          # 遍历路径清单
├── feature-manual.md           # 功能操作手册
├── api-mapping.md              # 接口映射清单
├── task-list.md                # 任务计划清单
└── screenshots/                # 截图素材
```

## 最佳实践

1. **按序执行**：遵循 ①→②→③→...→⑩ 的顺序，确保每步产出物完整
2. **及时归档**：每个阶段完成后，文档自动归档到对应 `docs/` 子目录
3. **版本管理**：设计文档支持版本迭代（V1、V2...）
4. **TDD 模式**：⑥ 执行开发时，推荐先写测试后写实现
5. **覆盖率要求**：单元测试覆盖率需 > 80%
6. **增量更新**：需求变更时使用 `/speckit.update` 智能更新，无需逐步执行 10 个流程
7. **迭代归档**：迭代完成后使用 `/speckit.archive` 归档，保持活跃目录精简

## 相关文档

- [项目规约](/.specify/memory/constitution.md)
- [C4 Model 规范](/.specify/memory/c4-model-constitution.md)
- [逆向工程规约](/.specify/memory/reverse-engineering-constitution.md)
