# {系统名称} - 接口映射清单

> 生成时间：{日期}
> 规约版本：v1.0
> 捕获接口数：{数量}

## 文档说明

本清单基于 Playwright 浏览器代理操作过程中捕获的真实接口调用，所有接口均有对应的触发操作和截图素材。

**核心原则**：仅记录实际捕获的接口，不推测不补充

## 接口总览

| 编号 | 接口路径 | 方法 | 触发操作 | 节点标识 | 素材编号 |
|------|----------|------|----------|----------|----------|
| API-001 | {路径} | GET/POST | {操作描述} | {节点标识} | {编号} |
| API-002 | {路径} | GET/POST | {操作描述} | {节点标识} | {编号} |

## 接口分类

### 按模块分类

| 模块 | 接口数 | 接口编号 |
|------|--------|----------|
| {模块1} | {数量} | API-001, API-002 |
| {模块2} | {数量} | API-003, API-004 |

### 按方法分类

| 方法 | 数量 |
|------|------|
| GET | {数量} |
| POST | {数量} |
| PUT | {数量} |
| DELETE | {数量} |

---

## 接口详情

### API-001: {接口名称}

| 属性 | 值 |
|------|------|
| **URL** | `{完整路径}` |
| **Method** | {GET/POST/PUT/DELETE} |
| **触发操作** | {操作描述} |
| **节点标识** | {节点标识} |
| **素材编号** | {编号} |
| **捕获时间** | {时间} |

**触发截图**：

![触发操作](./screenshots/{素材编号}.png)

**Request Headers**：

```json
{
  "Authorization": "Bearer ***（已脱敏）",
  "Content-Type": "application/json",
  "...": "..."
}
```

**Request Body**（如有）：

```json
{
  "param1": "value1",
  "param2": "value2"
}
```

**Response**（Status: {状态码}）：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "...": "..."
  }
}
```

**字段说明**：

| 字段 | 类型 | 说明 |
|------|------|------|
| {字段名} | {类型} | {说明} |

---

### API-002: {接口名称}

...

---

## 附录

### A. 接口-功能映射表

| 功能点 | 相关接口 |
|--------|----------|
| {功能点1} | API-001, API-002 |
| {功能点2} | API-003 |

### B. 未捕获接口说明

如有预期存在但未捕获的接口，可能原因：
- 操作未触发（如置灰按钮）
- 权限不足
- 前端缓存

| 预期功能 | 可能接口 | 未捕获原因 |
|----------|----------|------------|
| {功能} | {推测，仅供参考} | {原因} |
